@model Loyalty.Web.ViewModels.ShopViewModel



<div class="col-md-4">
    <h3>@Model.Name</h3>
    <p>
        @Model.Adress
    </p>
    @if (this.User.Identity.IsAuthenticated)
    {
        <p>
            @Html.ActionLink("Details", "Details", "Shop", new { id = Model.Id }, new { @class = "btn btn-default" })
        </p>
    }

</div>