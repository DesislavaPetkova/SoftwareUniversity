@using PagedList.Mvc
@model PagedList.IPagedList<Twitter.Web.ViewModels.TweetViewModel>
@{
    ViewBag.Title = "User's Homepage";
    
}

<h2>@ViewBag.Title</h2>

<br />
<div class="row">
    @Html.DisplayFor(x => x)
</div>

@Html.PagedListPager(Model, page => Url.Action("Index", new { page }))